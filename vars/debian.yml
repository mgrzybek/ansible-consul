consul_distro_packages:
- consul

consul_sysconfig: /etc/default/consul
consul_conf_file: /etc/consul.d/consul.hcl

consul_apt_key: https://apt.releases.hashicorp.com/gpg
consul_apt_repo: deb [arch=amd64] https://apt.releases.hashicorp.com {{ ansible_distribution_release }} main
